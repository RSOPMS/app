apiVersion: v2
name: bugbase
description: A Helm chart for Kubernetes

# A chart version is always required
version: 0.1.0

# This is the version of the application being deployed.
appVersion: "1.0.0"
